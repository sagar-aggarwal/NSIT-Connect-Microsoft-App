<Page
    x:Class="NSIT_Connect.Views.HangoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NSIT_Connect.Views"
    xmlns:ViewModel ="using:NSIT_Connect.ViewModels"
    xmlns:models="using:NSIT_Connect.Models"
    xmlns:Controls="using:NSIT_Connect.Controls"
    xmlns:Rating="using:Bratched.Tools.RatingControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:HangoutPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Page.BottomAppBar>
        <CommandBar Background="{StaticResource colorPrimaryDark}" Foreground="White" Opacity="0.65" >
            <AppBarButton Icon="Shuffle" Label="Shuffle" />
            <AppBarButton Icon="RepeatAll" Label="Repeat" />
            <AppBarSeparator/>
            <AppBarButton Icon="Back" Label="Back" />
        </CommandBar>
    </Page.BottomAppBar>


    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Custom.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock 
                Grid.Row="0"
                Text="{x:Bind ViewModel.Selected.Name}"
        		Style="{ThemeResource BodyTextBlockStyle}"
                Margin="10,0,5,0"
                FontSize="55"
        		Foreground="{StaticResource colorPrimaryDark}" />


        <GridView Margin="10,25,5,0" Grid.Row="1" ItemsSource="{x:Bind ViewModel.Item}" HorizontalAlignment="Stretch">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:HangoutItem">
                    <Controls:HangoutPanel Height="180" Width="402"  PlaceName="{x:Bind Name}" PlaceVicinity="{x:Bind Vicinity}" RatingValue="{x:Bind Rating}" PlaceAvailable="{x:Bind OpenNowString}" Source="{x:Bind Photo_Ref}" />
                </DataTemplate>
            </GridView.ItemTemplate>

        </GridView>

    </Grid>


</Page>
